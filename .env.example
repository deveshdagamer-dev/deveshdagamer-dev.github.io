# Example environment file.
# Copy to a local .env file only when a backend/serverless provider is added.
# Never commit real API keys, tokens, passwords, webhook URLs, or private keys.

# PUBLIC_SITE_URL=https://deveshbinwal.com
# PRIVATE_API_KEY=replace_with_server_side_secret
# RATE_LIMIT_REQUESTS_PER_MINUTE=10
